# ── Ce que la construction fabrique ────────────────────────────────────────
build/
*.elf
*.nds
*.map
*.o

# ── Ce que le tracker EXPORTE ──────────────────────────────────────────────
# Cartouches et VGM produits par les pages EXPORT : ce sont des sorties, elles
# se refabriquent depuis le morceau.
*.BIN
*.VGM
*.bin
*.vgm

# ── L'image de carte SD de l'emulateur ─────────────────────────────────────
# 64 Mo de donnees d'essai, sans rapport avec le code.
carteSD/

# ── Le journal du tracker ──────────────────────────────────────────────────
GENETRACKER.LOG

.DS_Store

# La musique de l auteur et les morceaux importes : des compositions,
# pas des sources.
*.dmf
*.DMF
*.a2m
*.A2M
*.mdm
*.MDM
data/

# Le binaire publie va sur la page Releases, pas dans le depot.
release/
*.nds

# Les notes de travail : francaises, et elles decrivent l interieur du
# projet iPad, qui est proprietaire.
LISEZMOI.md
